apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: ignite-role
rules:
  - apiGroups:
      - ""
    resources:
      - endpoints
      - pods
    verbs:
      - get
      - list
      - watch